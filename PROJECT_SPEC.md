# 小熊猪银行｜PROJECT SPEC

## 1. 项目基本信息

* 项目名称：小熊猪银行
* 英文名称：Kuma & Piggy Bank
* 项目类型：情侣双人虚拟银行网页
* 使用对象：仅供“小熊”和“小猪”两位用户共同使用
* 第一版形式：响应式网页
* 第一版数据存储：浏览器 LocalStorage
* 默认语言：简体中文
* 主要使用设备：手机，同时兼容电脑浏览器

---

## 2. 项目目的

“小熊猪银行”是一个为情侣设计的双币虚拟银行系统。

它不是普通的人民币记账工具，也不涉及真实货币支付。它通过“熊币”和“猪币”记录两个人在关系中的额外付出、关心、陪伴、惊喜和共同活动，并允许双方使用获得的虚拟币兑换情侣奖励。

项目希望实现以下目标：

1. 记录两个人值得被看见和感谢的行为。
2. 通过虚拟币奖励增加情侣互动的趣味性。
3. 建立清晰、可追溯的双人银行流水。
4. 提供一个可兑换陪伴、服务、约会和小惊喜的奖励商店。
5. 保存两个人共同经营银行的纪念、目标和成就。
6. 营造类似日系个人主页、电子手账和情侣秘密基地的使用氛围。
7. 避免将基本尊重、正常沟通和感情义务过度交易化。

项目整体感觉应当是：

> 一间由小熊和小猪共同经营的、可爱而认真、带有纪念意义的情侣银行。

---

## 3. 用户和角色

系统中固定存在两位角色。

### 3.1 小熊

* 显示名称：小熊
* 英文标识：Kuma
* 角色图标：原创小熊图标
* 主要代表货币：熊币
* 可以接受小猪给小熊发放的熊币
* 可以提供奖励商品
* 可以确认或拒绝小猪发起的相关记录和兑换

### 3.2 小猪

* 显示名称：小猪
* 英文标识：Piggy
* 角色图标：原创小猪图标
* 主要代表货币：猪币
* 可以接受小熊给小猪发放的猪币
* 可以提供奖励商品
* 可以确认或拒绝小熊发起的相关记录和兑换

### 3.3 第一版身份切换方式

第一版不做真实账户注册和登录。

页面顶部或侧边栏提供一个简单的身份切换器：

* 当前身份：小熊
* 当前身份：小猪

身份切换只用于模拟两个人分别操作网页。

LocalStorage 中保存当前选择的身份。

---

## 4. 货币规则

系统存在两种虚拟货币。

### 4.1 熊币

* 中文名称：熊币
* 英文标识：Bear Coin
* 代码标识：BEAR
* 图标建议：小熊头像、蜂蜜罐或熊掌硬币
* 主要发行者：小猪
* 主要获得者：小熊

### 4.2 猪币

* 中文名称：猪币
* 英文标识：Pig Coin
* 代码标识：PIG
* 图标建议：小猪头像、蝴蝶结或猪鼻硬币
* 主要发行者：小熊
* 主要获得者：小猪

### 4.3 固定折算关系

固定折算关系为：

> 1熊币 = 5猪币

第一版中，该汇率主要用于：

1. 展示两种币的价值关系。
2. 计算账户折算总值。
3. 为奖励商品展示另一币种的参考价格。

### 4.4 第一版不开放自由换币

第一版不实现熊币和猪币之间的自由双向兑换。

例如：

* 用户不能直接将2熊币变成10猪币。
* 用户不能通过重复兑换改变账户结构。

奖励商品可以设置：

* 只接受熊币
* 只接受猪币
* 同时提供熊币价和猪币价

实际支付时只扣除用户选择的币种。

### 4.5 初始余额

第一版默认初始余额：

* 小熊：0熊币、0猪币
* 小猪：0熊币、0猪币

项目可以提供一组演示数据，但用户点击“重置银行数据”后，应恢复为零余额。

### 4.6 余额生成原则

用户不能直接编辑余额。

余额必须由全部有效交易流水自动计算得出。

```text
当前余额 = 所有已生效收入之和 - 所有已生效支出之和
```

余额卡片只能展示余额，不能直接修改数字。

### 4.7 负余额规则

第一版不允许余额小于0。

当用户余额不足时：

* 禁止完成奖励兑换。
* 显示余额不足提示。
* 不创建扣款交易。
* 不创建兑换订单。

### 4.8 自己给自己发币

第一版不允许用户直接给自己发放虚拟币。

默认规则：

* 小熊可以向小猪发放猪币。
* 小猪可以向小熊发放熊币。

如需记录双方共同完成的活动，可以创建“双人共同奖励”，分别产生两笔交易。

### 4.9 扣币和惩罚

第一版不允许用户因为争吵、情绪或不满意而直接扣除对方已有的虚拟币。

虚拟币主要用于正向记录和奖励，不作为惩罚工具。

允许减少余额的情况包括：

* 兑换奖励。
* 向共同金库存入虚拟币。
* 撤销一笔错误收入。
* 对已取消的交易进行反向处理。

---

## 5. 银行核心原则

项目必须遵守以下数据和关系原则。

### 5.1 所有余额变化必须有流水

任何导致余额增加或减少的行为，都必须生成一条交易记录。

不能只修改余额而不留下原因和时间。

### 5.2 流水不能直接删除

已创建的交易原则上不允许被彻底删除。

如果一笔交易填写错误，应使用“撤销交易”。

撤销后：

1. 原交易保留。
2. 原交易状态变为“已撤销”。
3. 系统自动创建一笔金额相反的撤销交易。
4. 余额通过反向交易恢复。

### 5.3 重要操作需要对方确认

第一版支持以下两种确认模式：

* 无需确认：提交后立即生效。
* 需要确认：对方确认后才生效。

需要确认的交易在确认前状态为：

```text
pending
```

在确认前不计入有效余额。

### 5.4 不将基本感情责任商品化

以下内容不应作为必须支付虚拟币才能获得的奖励：

* 基本尊重
* 忠诚
* 正常沟通
* 道歉
* 不欺骗
* 不冷暴力
* 身体和亲密行为的同意
* 原谅
* 基本安全感
* 正常回应对方的合理需求

奖励应主要围绕：

* 额外陪伴
* 小惊喜
* 主动服务
* 约会体验
* 娱乐选择权
* 共同愿望
* 纪念活动

---

## 6. 页面结构

第一版建立五个主要页面。

---

## 6.1 银行首页

页面名称：

* 中文：银行首页
* 英文：Bank Home
* 路由建议：`/`

首页用于展示最重要的银行信息。

### 首页必须包含

#### A. 顶部银行横幅

显示：

* 小熊猪银行
* Kuma & Piggy Bank
* 1熊币 = 5猪币
* 银行成立日期
* 今日营业状态
* 当前用户身份切换

示例文案：

> 今日营业中 ♡
> 认真保存每一份爱。

#### B. 双人账户卡片

分别展示：

* 小熊头像
* 小熊的熊币余额
* 小熊的猪币余额
* 按固定汇率折算后的账户总值
* 小猪头像
* 小猪的熊币余额
* 小猪的猪币余额
* 按固定汇率折算后的账户总值

折算总值可以统一折算为熊币：

```text
折算总值 = 熊币余额 + 猪币余额 ÷ 5
```

折算结果仅用于展示，不改变真实余额。

#### C. 本周数据

展示：

* 本周获得的熊币
* 本周获得的猪币
* 本周兑换次数
* 本周已完成奖励数量

#### D. 快捷操作

提供以下按钮：

* 记录一笔奖励
* 去奖励商店
* 查看流水
* 查看待确认事项

#### E. 最近流水

展示最近3至5笔交易。

每条显示：

* 交易原因
* 金额
* 币种
* 时间
* 状态

#### F. 待确认事项

展示：

* 待确认的发币记录
* 待确认的奖励兑换
* 待确认的退款申请

#### G. 共同目标

展示至少一个共同目标：

* 目标名称
* 目标金额
* 当前进度
* 进度条
* 查看详情按钮

第一版可以使用模拟目标：

> 周末特别约会基金

---

## 6.2 赚币记录

页面名称：

* 中文：赚币记录
* 英文：Earn Coins
* 路由建议：`/earn`

该页面用于记录某个人为什么获得虚拟币。

### 表单字段

#### 必填字段

* 发起人
* 获得奖励的人
* 币种
* 数量
* 行为分类
* 奖励原因
* 日期和时间
* 是否需要对方确认

#### 可选字段

* 详细备注
* 纪念照片占位
* 自定义标签

### 默认行为分类

* 主动关心
* 认真倾听
* 提供帮助
* 制造惊喜
* 陪伴学习
* 承担家务
* 完成约定
* 共同活动
* 矛盾修复
* 特别纪念
* 其他

### 提交规则

提交后创建一笔交易。

如果无需确认：

```text
status = confirmed
```

交易立即计入余额。

如果需要确认：

```text
status = pending
```

交易暂时不计入余额，等待对方确认。

### 表单校验

* 数量必须大于0。
* 数量必须为整数。
* 发起人与获得者不能相同。
* 奖励原因不能为空。
* 不允许提交无法识别的币种。
* 提交成功后显示明确提示。
* 防止用户短时间内重复提交同一笔记录。

---

## 6.3 奖励商店

页面名称：

* 中文：奖励商店
* 英文：Reward Shop
* 路由建议：`/rewards`

奖励商店展示可以使用虚拟币兑换的情侣奖励。

### 奖励分类

* 小小奖励
* 陪伴服务
* 特别权限
* 约会体验
* 长期愿望

### 每张奖励卡片包含

* 奖励名称
* 奖励图标
* 奖励分类
* 简短说明
* 提供者
* 兑换者
* 熊币价格
* 猪币价格
* 每周或每月兑换上限
* 是否需要提前预约
* 预计履约时间
* 当前是否可以兑换
* 兑换按钮

### 第一版示例奖励

#### 小小奖励

1. 十分钟认真抱抱

   * 价格：1熊币或5猪币
   * 提供者：对方
   * 每周限兑2次

2. 今日夸夸券

   * 价格：1熊币或5猪币
   * 提供者：对方
   * 每日限兑1次

3. 今日选歌权

   * 价格：1熊币或5猪币

#### 陪伴服务

1. 陪散步半小时

   * 价格：3熊币或15猪币

2. 十五分钟肩颈按摩

   * 价格：4熊币或20猪币

3. 陪伴完成一个学习任务

   * 价格：5熊币或25猪币

4. 帮拿一次外卖

   * 价格：2熊币或10猪币

#### 特别权限

1. 今晚电影选择权

   * 价格：3熊币或15猪币

2. 下一次餐厅选择权

   * 价格：5熊币或25猪币

3. 一次约会主题决定权

   * 价格：8熊币或40猪币

#### 约会体验

1. 不看手机的认真约会

   * 价格：10熊币或50猪币

2. 对方策划一次小约会

   * 价格：15熊币或75猪币

3. 一顿特别晚餐

   * 价格：20熊币或100猪币

#### 长期愿望

1. 周末短途旅行

   * 价格：50熊币或250猪币

2. 情侣写真

   * 价格：60熊币或300猪币

长期愿望第一版可以作为展示或共同目标，不必全部支持直接兑换。

### 奖励兑换流程

兑换流程如下：

```text
选择奖励
→ 选择支付币种
→ 检查余额
→ 创建兑换订单
→ 扣除对应余额
→ 等待提供者确认
→ 待履约
→ 已完成
```

### 兑换状态

奖励兑换订单存在以下状态：

* `pending_confirmation`：等待提供者确认
* `accepted`：已接受，等待履约
* `completed`：已经完成
* `rejected`：已拒绝，并退款
* `cancelled`：兑换者主动取消，并退款
* `refunded`：已经退款
* `expired`：超过履约期限

### 扣款和退款规则

创建兑换订单时：

1. 检查余额是否足够。
2. 创建一笔支出交易。
3. 支出交易与兑换订单关联。
4. 如果兑换被拒绝或取消，创建一笔金额相反的退款交易。
5. 原支出交易不删除。

---

## 6.4 流水账本

页面名称：

* 中文：流水账本
* 英文：Transactions
* 路由建议：`/transactions`

该页面展示所有影响余额或订单状态的记录。

### 流水列表字段

* 日期和时间
* 交易类型
* 发起人
* 收款人或付款人
* 币种
* 金额
* 原因
* 状态
* 关联奖励
* 是否已撤销
* 查看详情按钮

### 交易类型

* 行为奖励
* 奖励兑换
* 赠送虚拟币
* 双人共同奖励
* 共同金库存入
* 共同金库退款
* 兑换退款
* 撤销交易
* 系统初始化
* 其他

### 筛选功能

提供以下筛选：

* 全部
* 收入
* 支出
* 待确认
* 奖励兑换
* 退款
* 已撤销
* 熊币
* 猪币
* 小熊
* 小猪

### 搜索功能

支持按以下内容搜索：

* 奖励原因
* 奖励名称
* 行为分类
* 用户名称
* 日期

### 交易详情

点击一笔流水后，可以查看：

* 完整原因
* 创建时间
* 确认时间
* 发起人
* 确认人
* 关联订单
* 撤销记录
* 备注

---

## 6.5 我们的小屋

页面名称：

* 中文：我们的小屋
* 英文：Our Room
* 路由建议：`/our-room`

该页面是情侣共同空间，视觉上可以比银行页面更自由、更像个人主页和电子手账。

### 必须包含

#### A. 银行资料

* 银行名称
* 银行成立日期
* 已经营天数
* 固定汇率
* 两位成员
* 银行介绍

#### B. 共同愿望

展示：

* 愿望名称
* 愿望说明
* 目标金额
* 当前金额
* 完成进度
* 预计实现日期

#### C. 情侣纪念

第一版使用占位卡片：

* 第一次见面的日期
* 银行成立纪念
* 第一次奖励兑换
* 已完成的特别约会

第一版暂不实现真实图片上传，但可以提供本地图片占位。

#### D. 银行成就

示例：

* 完成第一笔交易
* 累计完成10笔交易
* 完成第一次奖励兑换
* 连续经营7天
* 累计获得100猪币
* 累计获得20熊币
* 完成第一次共同目标

#### E. 月度小报告

展示模拟或根据本地数据自动计算的内容：

* 本月交易总数
* 本月获得的熊币
* 本月获得的猪币
* 最常出现的奖励原因
* 最受欢迎的奖励
* 已完成奖励数
* 未完成奖励数

#### F. 银行规则

展示本文件中最重要的用户规则，例如：

* 不允许给自己发币。
* 不允许随意扣除对方余额。
* 所有余额变化必须有流水。
* 错误交易只能撤销，不能直接删除。
* 奖励未完成时可以退款。
* 基本尊重和爱不能通过虚拟币购买。

---

## 7. 待确认中心

待确认中心可以作为首页中的面板，也可以制作独立弹窗或抽屉。

第一版不强制建立独立路由。

### 待确认事项包括

* 待确认的奖励收入
* 待接受的奖励兑换
* 退款申请
* 撤销申请

### 可执行操作

* 确认
* 拒绝
* 查看详情

### 确认规则

只有与该事项相关的另一方可以确认。

例如：

* 小熊向小猪发放熊币时，小猪可以确认。
* 小猪兑换小熊提供的奖励时，小熊可以接受或拒绝。

---

## 8. 数据结构

第一版数据统一保存在 LocalStorage 中。

建议使用一个总对象：

```ts
interface BankState {
  version: number;
  currentUserId: UserId;
  users: User[];
  transactions: Transaction[];
  rewards: Reward[];
  redemptions: Redemption[];
  goals: SharedGoal[];
  achievements: Achievement[];
  settings: BankSettings;
}
```

---

## 8.1 用户数据

```ts
type UserId = "bear" | "pig";

interface User {
  id: UserId;
  name: string;
  englishName: string;
  avatar: string;
  primaryCurrency: CurrencyCode;
}
```

默认数据：

```ts
const users = [
  {
    id: "bear",
    name: "小熊",
    englishName: "Kuma",
    avatar: "/assets/bear-avatar.svg",
    primaryCurrency: "BEAR"
  },
  {
    id: "pig",
    name: "小猪",
    englishName: "Piggy",
    avatar: "/assets/pig-avatar.svg",
    primaryCurrency: "PIG"
  }
];
```

---

## 8.2 币种数据

```ts
type CurrencyCode = "BEAR" | "PIG";

interface Currency {
  code: CurrencyCode;
  name: string;
  symbol: string;
  exchangeValueInPigCoins: number;
}
```

默认数据：

```ts
const currencies = [
  {
    code: "BEAR",
    name: "熊币",
    symbol: "🐻",
    exchangeValueInPigCoins: 5
  },
  {
    code: "PIG",
    name: "猪币",
    symbol: "🐷",
    exchangeValueInPigCoins: 1
  }
];
```

---

## 8.3 交易数据

```ts
type TransactionStatus =
  | "pending"
  | "confirmed"
  | "rejected"
  | "reversed";

type TransactionType =
  | "behavior_reward"
  | "reward_purchase"
  | "gift"
  | "shared_reward"
  | "goal_deposit"
  | "refund"
  | "reversal"
  | "system";

interface Transaction {
  id: string;
  createdAt: string;
  effectiveAt?: string;

  type: TransactionType;
  status: TransactionStatus;

  createdBy: UserId;
  ownerId: UserId;

  currency: CurrencyCode;
  amount: number;

  category?: string;
  reason: string;
  note?: string;

  requiresConfirmation: boolean;
  confirmedBy?: UserId;
  confirmedAt?: string;

  relatedRewardId?: string;
  relatedRedemptionId?: string;
  reversedTransactionId?: string;

  isReversal: boolean;
}
```

### 金额规则

* 收入交易使用正数。
* 支出交易使用负数。
* 待确认交易暂时不计入余额。
* 已拒绝交易不计入余额。
* 已撤销交易通过反向流水抵消。

---

## 8.4 奖励商品数据

```ts
type RewardCategory =
  | "small"
  | "companionship"
  | "privilege"
  | "date"
  | "long_term";

interface Reward {
  id: string;
  title: string;
  description: string;
  category: RewardCategory;
  icon: string;

  providerId: UserId;

  priceBear?: number;
  pricePig?: number;

  limitPeriod?: "day" | "week" | "month" | "none";
  limitCount?: number;

  requiresAppointment: boolean;
  fulfilmentDays?: number;

  isActive: boolean;
  createdAt: string;
}
```

---

## 8.5 兑换订单数据

```ts
type RedemptionStatus =
  | "pending_confirmation"
  | "accepted"
  | "completed"
  | "rejected"
  | "cancelled"
  | "refunded"
  | "expired";

interface Redemption {
  id: string;
  rewardId: string;

  redeemerId: UserId;
  providerId: UserId;

  paymentCurrency: CurrencyCode;
  paymentAmount: number;

  status: RedemptionStatus;

  createdAt: string;
  acceptedAt?: string;
  completedAt?: string;
  cancelledAt?: string;

  note?: string;

  paymentTransactionId: string;
  refundTransactionId?: string;
}
```

---

## 8.6 共同目标数据

```ts
interface SharedGoal {
  id: string;
  title: string;
  description?: string;

  targetCurrency: CurrencyCode;
  targetAmount: number;

  deadline?: string;
  icon?: string;

  status: "active" | "completed" | "cancelled";
  createdAt: string;
}
```

共同目标的当前金额不得直接储存为最终真值，应通过相关的共同金库交易自动计算。

---

## 8.7 成就数据

```ts
interface Achievement {
  id: string;
  title: string;
  description: string;
  icon: string;

  unlocked: boolean;
  unlockedAt?: string;

  ruleType: string;
  ruleValue: number;
}
```

---

## 8.8 系统设置

```ts
interface BankSettings {
  bankName: string;
  bankEnglishName: string;
  foundedAt: string;

  bearToPigRate: number;

  allowNegativeBalance: false;
  allowSelfReward: false;
  allowDirectCurrencyExchange: false;

  theme: "kuma-retro";
}
```

---

## 9. 余额计算逻辑

必须建立统一余额计算函数。

```ts
function calculateBalance(
  transactions: Transaction[],
  userId: UserId,
  currency: CurrencyCode
): number
```

计算时只包含：

```text
status === "confirmed"
```

并且交易的：

```text
ownerId === userId
currency === 指定币种
```

余额为有效交易金额之和。

不得在用户对象中保存一个可以被随意编辑的余额字段。

---

## 10. 视觉风格

参考视觉氛围：

* 2000年代日系个人主页
* Neocities个人网站
* Webcore
* 电子手账
* 复古情侣秘密基地
* 可爱的小型银行营业厅

参考网页：

```text
https://kumaworld.neocities.org/
```

只学习整体视觉语言和版式氛围，不复制：

* 原网站源码
* 原网站文字
* 原网站横幅
* 原网站图片
* 原网站GIF
* Rilakkuma版权素材
* 原作者独有的装饰组合

所有角色、硬币、贴纸和图标必须使用：

* 原创SVG
* CSS绘制图形
* 开源图标
* 临时占位图

---

## 10.1 色彩系统

建议使用以下色彩方向：

```css
:root {
  --color-cream: #fffaf0;
  --color-paper: #fffdf8;
  --color-pink: #f6cbd4;
  --color-light-pink: #fde8ed;
  --color-blue: #cfe5ef;
  --color-light-blue: #edf7fa;
  --color-green: #d8e8cf;
  --color-yellow: #f7e5a7;
  --color-caramel: #b9855b;
  --color-brown: #6f5142;
  --color-red: #c96c72;
  --color-border: #b9957b;
  --color-text: #59483f;
}
```

以上颜色允许微调，但不得改成：

* 深色科技风
* 金融蓝渐变
* 黑金奢华风
* 玻璃拟态
* 赛博朋克
* 大面积霓虹色

---

## 10.2 页面布局

### 桌面端

* 页面主体居中。
* 建议最大宽度为1000至1150像素。
* 使用复古两栏布局。
* 左侧为账户、汇率、公告和小组件。
* 右侧为当前页面主要内容。
* 页面背景可以使用浅色格纹、点阵或小图案。
* 内容卡片使用细边框、虚线框和小标题栏。

### 手机端

* 自动切换为单栏。
* 账户卡片上下排列。
* 表格允许横向滚动或改为卡片。
* 使用底部固定导航。
* 常用按钮需要适合手指点击。
* 不得因为保留桌面版复古布局而导致文字过小。

---

## 10.3 视觉元素

可以使用：

* 小熊和小猪原创头像
* 熊币和猪币SVG
* 小星星
* 小爱心
* 蜂蜜罐
* 蝴蝶结
* 邮票
* 信封
* 银行存折
* 票券
* 印章
* 小日历
* 格纹纸张
* 便签纸
* 虚线剪裁边
* 像素感小标题
* 少量闪烁或漂浮动画

动画必须轻微，不能影响阅读。

需要尊重：

```css
@media (prefers-reduced-motion: reduce)
```

当用户关闭动画时，应减少或停止装饰动画。

---

## 10.4 字体

* 正文优先使用清晰的中文系统字体。
* 标题可以使用少量像素风或手写风字体。
* 不允许所有正文都使用难以阅读的装饰字体。
* 不依赖用户本地不存在的字体文件。
* 不在项目中加入未经授权的商业字体。

---

## 11. 导航结构

桌面端顶部或侧边导航：

```text
BANK HOME
银行首页

EARN COINS
赚币记录

REWARD SHOP
奖励商店

TRANSACTIONS
流水账本

OUR ROOM
我们的小屋
```

手机端底部导航只展示：

* 首页
* 赚币
* 奖励
* 流水
* 我们

当前页面需要有明确选中状态。

---

## 12. LocalStorage要求

建议使用以下键名：

```text
kuma-piggy-bank-state
```

必须实现：

1. 首次访问时载入默认数据。
2. 每次数据修改后自动保存。
3. 页面刷新后数据仍然存在。
4. 提供“导出演示数据”或“重置数据”按钮。
5. 重置前必须出现二次确认。
6. 数据结构包含版本号。
7. 加载旧版本数据时保留迁移接口。
8. LocalStorage读取失败时使用安全默认值。
9. 不允许因为损坏数据导致整个页面白屏。

---

## 13. 第一版必须实现的功能

第一版 MVP 必须完成：

* 五个主要页面。
* 小熊和小猪身份切换。
* 响应式桌面和手机布局。
* 原创复古日系视觉风格。
* 新增行为奖励记录。
* 待确认交易。
* 确认或拒绝交易。
* 根据流水自动计算余额。
* 奖励商品展示。
* 余额足够时兑换奖励。
* 奖励兑换订单状态。
* 拒绝或取消兑换时自动退款。
* 完整流水列表。
* 流水筛选。
* 撤销错误交易。
* 共同目标展示。
* 月度数据统计。
* LocalStorage数据保存。
* 示例数据重置。
* 空数据状态。
* 错误提示。
* 成功提示。
* 基础表单校验。
* 手机端底部导航。
* README运行说明。

---

## 14. 第一版明确不做的功能

第一版不得主动增加以下功能：

### 账户和云端

* 不做真实邮箱注册。
* 不做手机号登录。
* 不做第三方登录。
* 不做Supabase。
* 不做Firebase。
* 不做服务器数据库。
* 不做多人账户系统。
* 不做跨设备实时同步。

### 真实货币

* 不接入微信支付。
* 不接入支付宝。
* 不接入银行卡。
* 不支持人民币充值。
* 不支持虚拟币提现。
* 不提供任何真实金融服务。

### 高级功能

* 不做聊天系统。
* 不做公开社交社区。
* 不做排行榜。
* 不做AI自动评价感情。
* 不做AI自动发币。
* 不做自动惩罚或扣币。
* 不做复杂投资理财。
* 不做利息。
* 不做贷款。
* 不做信用评分。
* 不做自由外汇交易。
* 不做复杂通货膨胀系统。
* 不做后台管理系统。
* 不做真实图片云端上传。
* 不做推送通知。
* 不做电子邮件通知。

### 视觉素材

* 不直接复制参考网站。
* 不使用Rilakkuma官方图片。
* 不使用未经授权的角色素材。
* 不使用大量外链GIF。
* 不为了复古风格牺牲手机可用性。

---

## 15. 代码和项目结构要求

建议项目结构：

```text
src/
├── assets/
│   ├── icons/
│   ├── illustrations/
│   └── patterns/
│
├── components/
│   ├── layout/
│   ├── bank/
│   ├── rewards/
│   ├── transactions/
│   ├── goals/
│   └── common/
│
├── pages/
│   ├── HomePage.tsx
│   ├── EarnPage.tsx
│   ├── RewardsPage.tsx
│   ├── TransactionsPage.tsx
│   └── OurRoomPage.tsx
│
├── data/
│   ├── defaultState.ts
│   └── demoData.ts
│
├── hooks/
│   ├── useBankState.ts
│   └── useLocalStorage.ts
│
├── services/
│   ├── transactionService.ts
│   ├── rewardService.ts
│   └── storageService.ts
│
├── types/
│   └── bank.ts
│
├── utils/
│   ├── balance.ts
│   ├── currency.ts
│   ├── dates.ts
│   └── validation.ts
│
├── styles/
│   ├── variables.css
│   ├── global.css
│   ├── layout.css
│   └── components.css
│
├── App.tsx
└── main.tsx
```

### 代码要求

* 使用React、Vite和TypeScript。
* 使用普通CSS或CSS Modules。
* 不使用大型UI组件库。
* 不使用不必要的状态管理框架。
* 业务逻辑不能全部堆在页面组件中。
* 余额计算必须放在独立函数中。
* 交易、兑换和退款逻辑必须相互分离。
* 重要数据类型使用TypeScript接口。
* 避免使用`any`。
* 对LocalStorage异常进行处理。
* 对核心逻辑编写基础测试。
* 保持代码容易继续修改。

---

## 16. 交互和提示要求

每个用户操作都需要明确反馈。

### 成功提示示例

* 奖励记录已提交。
* 已等待小熊确认。
* 已成功获得5猪币。
* 奖励兑换成功。
* 奖励已完成。
* 退款已返回账户。
* 交易已撤销。

### 错误提示示例

* 余额不足，暂时无法兑换。
* 奖励数量必须大于0。
* 奖励原因不能为空。
* 这笔记录已经提交，请勿重复操作。
* 当前身份没有权限确认该事项。
* 数据保存失败，请稍后重试。

### 空状态示例

* 这里还没有流水，记录第一份小小的心意吧。
* 奖励商店正在准备新的惊喜。
* 暂时没有需要确认的事项。
* 共同愿望还没有开始存币。

---

## 17. 演示数据要求

开发阶段提供少量演示数据，方便查看页面效果。

演示数据建议包含：

* 小熊账户有少量猪币。
* 小猪账户有少量熊币。
* 至少6笔不同类型的流水。
* 至少1笔待确认交易。
* 至少1个进行中的兑换订单。
* 至少1个已完成的兑换订单。
* 至少10个奖励商品。
* 至少1个共同目标。
* 至少3个已解锁成就。

演示数据必须能够一键清除。

---

## 18. 验收标准

完成第一版时，至少满足以下条件。

### 视觉

* 页面呈现明显的日系复古个人主页风格。
* 页面不是现代金融App风格。
* 小熊猪角色和硬币素材为原创或占位素材。
* 桌面端结构清晰。
* 手机端能够正常操作。
* 文字大小清晰可读。

### 功能

* 新增收入后，余额计算正确。
* 待确认收入在确认前不计入余额。
* 确认后余额立即更新。
* 拒绝后余额不变。
* 兑换奖励时正确扣款。
* 余额不足时不能兑换。
* 兑换被拒绝后正确退款。
* 撤销交易后正确创建反向流水。
* 原交易仍然保留。
* 刷新页面后数据仍然存在。
* 重置数据后恢复默认状态。

### 代码

* 项目可以正常安装依赖。
* 项目可以正常启动。
* 项目可以正常构建。
* 控制台没有明显报错。
* README包含运行方式。
* 核心余额和退款逻辑有基础测试。
* 代码结构符合本文件约定。

---

## 19. 开发顺序

Codex应按照以下顺序开发。

### 阶段一：基础视觉原型

先完成：

* 项目创建
* 全局样式
* 页面布局
* 导航
* 五个静态页面
* 桌面端和手机端适配
* 原创占位图标
* 演示数据展示

此阶段先不要实现复杂业务逻辑。

### 阶段二：银行数据逻辑

实现：

* LocalStorage
* 身份切换
* 新增交易
* 待确认交易
* 自动余额计算
* 流水筛选
* 撤销交易

### 阶段三：奖励兑换

实现：

* 奖励商品
* 余额检查
* 兑换订单
* 扣款
* 接受和拒绝
* 完成奖励
* 取消和退款

### 阶段四：共同空间和测试

实现：

* 共同目标
* 成就
* 月度统计
* 空状态
* 错误处理
* 核心测试
* README
* 构建检查

---

## 20. Codex工作方式

开始开发前：

1. 阅读完整的`PROJECT_SPEC.md`。
2. 检查当前项目已有文件。
3. 不要删除与本项目有关的用户文件。
4. 先给出简短实施计划。
5. 按开发阶段逐步实现。
6. 每完成一个阶段后运行项目或构建检查。
7. 不确定的小细节优先采用本文件中的默认规则。
8. 不要自行增加第一版明确不做的功能。
9. 不要用复杂技术替代简单可靠的实现。
10. 完成后总结修改的文件、实现的功能和仍未实现的内容。

---

## 21. 后续版本可能增加的内容

以下内容不属于第一版，只作为未来方向记录：

* 小熊和小猪真实登录
* Supabase云端数据库
* 两部手机数据同步
* 照片上传
* PWA安装
* 数据导入和导出
* 周报和月报图片生成
* 纪念日主题
* 自定义银行皮肤
* 自定义小熊和小猪头像
* 奖励预约日历
* 每周发币上限
* 共同金库
* 情侣留言板
* 随机奖励盲盒
* 银行纪念证书
* 新设备数据恢复

这些功能只能在第一版稳定完成后再考虑。

---

## 22. 当前默认产品决策

为了避免开发过程中反复猜测，第一版采用以下默认决定：

* 使用React、Vite、TypeScript和普通CSS。
* 数据保存在LocalStorage。
* 不实现真实登录。
* 通过身份切换模拟小熊和小猪。
* 1熊币固定等于5猪币。
* 第一版不允许自由换币。
* 不允许给自己发币。
* 不允许余额为负数。
* 不允许随意扣除对方已有余额。
* 所有余额从流水自动计算。
* 交易不能直接删除，只能撤销。
* 奖励兑换时先扣款。
* 兑换被拒绝或取消时产生退款流水。
* 重要记录可以设置为需要对方确认。
* 视觉参考Neocities日系个人主页风格。
* 不复制参考网页和版权角色素材。
* 手机可用性优先于完全复刻复古网页布局。

---

## 23. 一句话产品定义

> 小熊猪银行是一间由小熊和小猪共同经营的情侣虚拟银行，用熊币和猪币记录幸福的点点滴滴，并记录保存两个人在一起的种种纪念。
